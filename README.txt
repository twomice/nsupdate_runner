nsupdate_runner

Checks the current IP address using a remote URL, and if 
different from the most recently stored IP address, updates
a given DNS server using nsupdate.

Requires a valid dnssec-keygen private key, and proper DNS server
configuration, which are outside the scope of this documentation.
